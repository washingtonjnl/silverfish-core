"""Reference adapters: concrete implementations of the ports.

These are optional, swappable implementations (local disk storage, a
SQLite-Calibre repository, Calibre-binary converter/injector, an SMTP mailer, a
Z-Library data source, ...). Consumers pick which to plug into a
``LibraryService``.
"""
